# Titanic-Survival-prediction
A simple Titanic survival prediction project using Python and scikit-learn.  This repository loads the Titanic dataset, cleans and preprocesses passenger data, trains a logistic regression model, and evaluates its accuracy using a held-out test set. 
